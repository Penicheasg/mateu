/* @license GPL-2.0-or-later https://www.drupal.org/licensing/faq */
(function(Drupal){'use strict';Drupal.behaviors.bootstrap_barrio={attach:function(context,settings){var position=window.scrollY;window.addEventListener('scroll',function(){if(window.scrollY>50)document.querySelector('body').classList.add("scrolled");else document.querySelector('body').classList.remove("scrolled");var scroll=window.scrollY;if(scroll>position){document.querySelector('body').classList.add("scrolldown");document.querySelector('body').classList.remove("scrollup");}else{document.querySelector('body').classList.add("scrollup");document.querySelector('body').classList.remove("scrolldown");}position=scroll;});document.addEventListener('click',function(event){if(!event.target.matches('.dropdown-item a.dropdown-toggle'))return;event.preventDefault();toggle(event.target.next('ul'));event.stopPropagation();},false);var toggle=function(elem){if(window.getComputedStyle(elem).display==='block'){hide(elem);return;}show(elem);};var show=function(elem){elem.style.display='block';};var hide=function(elem){elem.style.display='none';};}};})(Drupal);;
(function($,Drupal,once){'use strict';Drupal.behaviors.amil_bootstrap={attach:function(context,settings){filtro();$('.paragraphs_type-tabs .nav-link').on('click',function(){filtro();});function filtro(){$('.tab-pane.active #faq-subject-filter').on('change',function(){const selectedSubject=$(this).val().toLowerCase();$('.tab-pane.active .accordion-item').each(function(){const subjects=$(this).data('subjects').toLowerCase().split(',');if(selectedSubject===''||subjects.includes(selectedSubject))$(this).show();else $(this).hide();});});}$('.paragraph--type--tabs',context).each(function(){const $paragraphTabs=$(this);const tabsContainer=once('amil-tabs-scroll',$paragraphTabs.find('.tabs-container .nav-tabs'),context);const prevButton=once('amil-tabs-prev',$paragraphTabs.find('.tabs-nav-prev'),context);const nextButton=once('amil-tabs-next',$paragraphTabs.find('.tabs-nav-next'),context);if(tabsContainer.length&&prevButton.length&&nextButton.length){const $tabsContainer=$(tabsContainer[0]);const $prevButton=$(prevButton[0]);const $nextButton=$(nextButton[0]);const toggleNavButtons=function(){const container=$tabsContainer[0];const hasHorizontalScroll=container.scrollWidth>container.clientWidth;if(hasHorizontalScroll){const isAtStart=container.scrollLeft===0;const isAtEnd=container.scrollLeft+container.clientWidth>=container.scrollWidth;$prevButton.toggle(!isAtStart);$nextButton.toggle(!isAtEnd);}else{$prevButton.hide();$nextButton.hide();}};$nextButton.off('click').on('click',function(){const scrollAmount=$tabsContainer.width()/2;$tabsContainer.animate({scrollLeft:$tabsContainer.scrollLeft()+scrollAmount},300,toggleNavButtons);});$prevButton.off('click').on('click',function(){const scrollAmount=$tabsContainer.width()/2;$tabsContainer.animate({scrollLeft:$tabsContainer.scrollLeft()-scrollAmount},300,toggleNavButtons);});toggleNavButtons();$(window).on('resize',toggleNavButtons);$tabsContainer.on('scroll',toggleNavButtons);}});$('.carousel',context).each(function(){const $carousel=$(this);const $pagination=$carousel.find('.pagination');const $items=$carousel.find('.carousel-item');if($pagination.length&&$items.length>0){const updatePagination=function(){const activeIndex=$items.index($carousel.find('.carousel-item.active'))+1;$pagination.text(`${activeIndex}/${$items.length}`);};$carousel.on('slid.bs.carousel',updatePagination);updatePagination();}});}};})(jQuery,Drupal,once);document.addEventListener('DOMContentLoaded',()=>{const myCarousel=document.querySelector('[data-custom-carousel]');const carouselItems=myCarousel.querySelector('.carousel-inner').querySelectorAll('.carousel-item');const buttomPause=document.querySelector('#pauseBtn');const carouselInstance=new bootstrap.Carousel(myCarousel,{interval:10000});let is_paused=false;function pauseSlide(){carouselInstance.pause();is_paused=true;buttomPause.textContent='Continuar';buttomPause.classList.add('start');buttomPause.classList.remove('pause');}function resumeSlide(){carouselInstance.cycle();is_paused=false;buttomPause.textContent='Pausar';buttomPause.classList.remove('start');buttomPause.classList.add('pause');}buttomPause.addEventListener('click',function(){if(is_paused)resumeSlide();else pauseSlide();});carouselItems.forEach((item)=>{item.addEventListener('focusin',()=>{if(!is_paused)pauseSlide();});});myCarousel.addEventListener("keydown",function(event){const carouselElement=myCarousel.querySelector('.carousel-inner');const activeElement=document.activeElement;if(event.key==="Tab"&&myCarousel.contains(activeElement)){if(event.shiftKey){if(isFirstSlide(carouselElement))return;event.preventDefault();carouselInstance.prev();}else{if(isLastSlide(carouselElement))return;event.preventDefault();carouselInstance.next();}myCarousel.addEventListener('slid.bs.carousel',function(){focusFirstInteractiveElement(myCarousel);},{once:true});}});function focusFirstInteractiveElement(container){const activeSlide=container.querySelector('.carousel-item.active');const focusableElements=activeSlide.querySelectorAll('a[href], button, input, textarea, select, details, [tabindex]:not([tabindex="-1"])');if(focusableElements.length>0)focusableElements[0].focus();}function isFirstSlide(carouselElement){const activeItem=carouselElement.querySelector('.carousel-item.active');const firstItem=carouselElement.querySelector('.carousel-item:first-child');return activeItem===firstItem;}function isLastSlide(carouselElement){const activeItem=carouselElement.querySelector('.carousel-item.active');const lastItem=carouselElement.querySelector('.carousel-item:last-child');return activeItem===lastItem;}});;
(function($,Drupal,once){Drupal.behaviors.betterExposedFilters={attach:function(context){$('.bef-tree input[type=checkbox], .bef-checkboxes input[type=checkbox]').change(function(){_bef_highlight(this,context);}).filter(':checked').closest('.form-item',context).addClass('highlight');}};function _bef_highlight(elem,context){$elem=$(elem,context);$elem.attr('checked')?$elem.closest('.form-item',context).addClass('highlight'):$elem.closest('.form-item',context).removeClass('highlight');}Drupal.behaviors.autosubmitExcludeTextfield={attach:function(context,settings){if(!settings.better_exposed_filters?.autosubmit_exclude_textfield)return;$(once('autosubmit-exclude-textfield','.bef-exposed-form',context)).each(function(){$(this).find('*[type="text"]').attr('data-bef-auto-submit-exclude','');});}};})(jQuery,Drupal,once);;
/* @license MIT https://github.com/michalsnik/aos/blob/master/LICENSE */
(function($,_,Drupal,drupalSettings){"use strict";Drupal.behaviors.scrollEffectsInit={attach:function(context,settings){AOS.init();}};})(window.jQuery,window._,window.Drupal,window.drupalSettings);;
